import styles from "@/app/ui/settings/settings.module.css"

const Settings = () => {
    return (
        <div className={styles.container}>
            <h1>Settings</h1>
        </div>
    )
}

export default Settings